import React  from 'react';

export default function TextBlock(){
    const word = ['commerce text text text', 'offers text text text', 'products text text text'];
    const items = word.map((text)=> <p>{TextBlock}</p>);
    return(<div>{items}</div>, document.getElementById("blockText"))
}
